{
  "name": "novawatch-release-notifications",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled server-side job that sends NovaWatch release and engagement push notifications via OneSignal - see send-release-notifications.js.",
  "main": "send-release-notifications.js",
  "scripts": {
    "start": "node send-release-notifications.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
